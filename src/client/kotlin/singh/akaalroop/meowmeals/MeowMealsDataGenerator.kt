package singh.akaalroop.meowmeals

import net.fabricmc.fabric.api.datagen.v1.DataGeneratorEntrypoint
import net.fabricmc.fabric.api.datagen.v1.FabricDataGenerator

/**
 * Entry point for data generation in the MeowMeals mod.
 * Used to define and run data generators (like models, loot tables, recipes, etc.).
 */
object MeowMealsDataGenerator : DataGeneratorEntrypoint { // This to generate data for the mod
    override fun onInitializeDataGenerator(fabricDataGenerator: FabricDataGenerator) {
    }
}